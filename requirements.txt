# Core runtime dependencies (see pyproject.toml for the authoritative list).
numpy>=1.24
opencv-python-headless>=4.8
scipy>=1.10
scikit-image>=0.21
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
pyyaml>=6.0

# Demonstration UI (spec section 39): streamlit run app/streamlit_app.py
streamlit>=1.30

# Testing.
pytest>=7.4

# Machine-learning framework — CPU-only wheel (~700 MB, fits Streamlit Cloud).
# pip selects the CPU build automatically on Linux (the cloud host); locally
# replace with the CUDA wheel from https://pytorch.org if a GPU is available.
torch>=2.0
